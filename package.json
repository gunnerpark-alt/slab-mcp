{
  "name": "slab-mcp",
  "version": "1.0.0-apikey",
  "description": "MCP server for analyzing Clay tables — schema, rows, errors, and enrichment debugging (API-key auth)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "keywords": ["clay", "mcp", "debugging"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  }
}
